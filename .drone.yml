kind: pipeline
name: cola_ci
type: exec

steps:
  - name: build
    image: node:16
    depends_on: [clone]
    commands:
      - echo '-----start------'
      - ls
      - npm install --registry https://registry.npm.taobao.org
      - npm run build
      - echo '-----end------'
      - ls

trigger:
  branch:
    - feat-hello
  event:
    - pull_request
    - push
